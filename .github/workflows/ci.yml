name: ML Pipeline CI with Dagger

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  dagger:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache: false

      - name: Run Dagger pipeline
        uses: dagger/dagger-for-github@v8.2.0
        with:
          workdir: go
          verb: run
          args: go run pipline.go
          version: "0.19.6"
      
      - name: Validate model
        uses: lasselundstenjensen/itu-sdse-project-model-validator@vmain
        with:
          model-path: model

      - name: Upload trained model
        uses: actions/upload-artifact@v4
        with:
          name: model
          path: model
